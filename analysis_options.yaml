# This project intentionally keeps its existing style rules disabled while the
# legacy source is migrated incrementally. Analyzer errors are still reported.
# include: package:flutter_lints/flutter.yaml
